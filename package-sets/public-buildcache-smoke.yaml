description: Minimal single-package set used for public buildcache smoke tests.
openssl: {}
variants:
  gcc-openmpi:
    mpi_provider: openmpi
    specs:
      - "pkgconf@2.5.1"
    views:
      serial:
        - "pkgconf"
  gcc-mpich:
    mpi_provider: mpich
    specs:
      - "pkgconf@2.5.1"
    views:
      serial:
        - "pkgconf"
